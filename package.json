{
  "name": "rewards-ledger-testing",
  "version": "0.1.0",
  "private": true,
  "description": "A rewards-points ledger reference implementation and the edge-case test suite that tries to break it",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fast-check": "^3.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
